# 橘味儿音乐服务器 v3.3.7

本热修复解决 MVSep Mega53 在真实歌曲上输出长度比输入分块少 336 个采样点时，
BS-RoFormer 0.1.6 在 overlap-add 阶段报张量长度不一致的问题。

- 已复现：请求 882000 个采样点，模型返回 881664 个采样点。
- 修复：按照模型、窗口和目标缓冲区共同可写的最短长度叠加。
- CUDA 与 CPU 使用同一修复路径。
- 安装程序不下载、不替换 PyTorch、CUDA 或 1.37GB Mega53 模型。
- 服务启动前会校验修复文件哈希，拒绝未打补丁的运行器。
- 三首试跑失败记录可重置后重新处理。

本版本只用于三首服务器试跑。确认三首完整发布到
`G:\JuweierMusicProcessed\01_Ready` 后，再进行批量处理优化。
