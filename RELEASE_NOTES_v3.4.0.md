# 橘味儿音乐 v3.4.0

本版把 Windows、Android 和 iOS 正式切换为“服务器预处理、客户端直接使用成品”的产品模式。

## 三端成品曲库

- 打开软件只同步服务器数据库中的已发布成品，不扫描手机、电脑或服务器硬盘。
- 断网时继续显示本地缓存；联网后按 `catalog_version` 增量刷新。
- 搜索、分类、歌手 A-Z 和歌曲数量全部来自服务器成品索引。
- 点击歌曲直接播放成品原音，并加载已生成的七轨音频、和弦、五线谱、六线谱、歌词时间轴和编配 MIDI。
- 手机端和 Windows 端不再向普通用户展示导入、扫描或现场 AI 处理入口。

## 演奏与歌词

- 独立电吉他使用已发布的 MVSep Mega 53-Stems 成品轨，不再由客户端临时分轨。
- 播放时同步高亮当前乐谱音符和对应歌词单元。
- 木吉他、电吉他、贝斯、鼓、钢琴及主旋律谱面直接读取服务器成品文件。

## 服务器

- 正式原版歌曲目录：`H:\juweier-music`。
- 正式成品目录：`G:\JuweierMusicProcessed\01_Ready`。
- 仅 `publish_status=已发布` 且文件存在的歌曲进入客户端成品曲库。
- 已发布歌曲优先使用 `final_audio_path` 或 `original_audio` 成品文件，歌词优先使用 `lyrics_timeline.json`。
- 保留批量后台生产能力以及 v3.3.6–v3.3.9 的 Mega53、RTX 3060 低显存和简体中文发布修复。

服务器升级后运行 `Start-Juweier-Server-v340.cmd`。服务器启动不会重新扫描整个硬盘；新增歌曲由服务器管理端批量预检、处理、校验并发布后，三端自动识别。
