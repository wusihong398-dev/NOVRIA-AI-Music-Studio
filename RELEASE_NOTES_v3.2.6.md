# 橘味儿音乐 v3.2.6 手机端可测试版

本版本优先解决 Android、iOS 无法进入和无法测试的问题，Windows 端仅做必要兼容更新。

## 手机端

- 登录页增加“先进入测试（无需验证码）”，短信服务未配置时也能测试曲库、导入、AI 流水线、播放和谱面。
- 曲库目录写入本机缓存，重新打开 App 时先显示上次歌曲列表，再在后台刷新。
- 所有手机曲库、歌曲处理和链接导入请求统一到 `/api/v1/library/mobile/...`。
- Cloudflare 返回 HTML、空内容或暂时性 502/503/504 时自动重试，并显示可执行的中文错误说明。
- 歌手首字母由服务器返回，中文歌手支持拼音首字母；手机字母栏保持横向滚动。
- 小屏幕上的分类、本地导入和链接导入改为自动换行，避免 iOS 出现竖排或挤压。
- 谱面数据新增逐字/逐词歌词时间轴；当前字、已唱字、未唱字按橙色主题显示。
- 每个旋律音符可携带对应歌词字，MusicXML 同步写入歌词。

## 服务器

- 启动后先提供 SQLite 缓存目录，再后台增量刷新 MP3 与 FLAC 两个服务器曲库。
- 默认端口统一为 8001，可通过 `JUWEIER_SERVER_PORT` 修改。
- 新增 MP3/FLAC 多曲库配置、曲库状态、歌曲数量和更新时间。
- 新增手机专用 catalog、audio、cover、process、import-url 路由，避免 Cloudflare 路由不一致。

## Windows

- 歌手分类默认不展开；搜索时才展开匹配结果。
- 增加歌手首字母筛选，并兼容服务器返回的中文拼音首字母。
- 版本、安装包、说明和构建脚本统一为 v3.2.6。
