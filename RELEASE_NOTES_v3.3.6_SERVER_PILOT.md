# 橘味儿音乐 v3.3.6 服务器三首试跑热修复

## 修复内容

- 修复 v3.3.5 三首歌都在 53.4% 失败的问题。
- 根因不是 CUDA、显存或模型损坏，而是旧 `bs-roformer-infer` 过滤了官方
  Mega53 配置中的 `mlp_expansion_factor: 2`，错误使用默认值 4，造成权重
  512 维与运行结构 1024 维不匹配。
- 固定使用官方 `openmirlab/bs-roformer-infer` 提交
  `b0f1386fcced25f559f3e61c9f08a73cd9bddf80`。
- 安装运行器时使用 `--no-deps`，不会替换已经安装好的 CUDA PyTorch。
- 保留已经校验通过的 1.37GB Mega53 权重和 YAML，不重复下载。
- 增加小型架构探针：必须得到 `[8, 4]`，证明 `mlp_expansion_factor=2`
  已传入模型；否则服务器拒绝启动处理。
- 健康检查不再只看模型文件，会同时检查运行器兼容标记。

## 仍需真实验证

本包已做代码与静态测试，但真实 Mega53 推理必须在服务器 RTX 3060 上重跑三首歌。
RTX 3060 12GB 低于上游对该 53 轨模型建议的 16GB 显存；如果 CUDA 显存不足，
程序会尝试 CPU，但会明显变慢。三首试跑成功前不要启动大批量任务。
