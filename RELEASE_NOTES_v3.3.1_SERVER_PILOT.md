# 橘味儿音乐 v3.3.1 服务器三首试跑版

本包只更新 Windows 服务器端，不更新手机或 Windows 客户端。

## 本次完成

- 从 `D:\MP3` 向下递归全部目录层级扫描音频，不限制歌手目录下的专辑、CD 或其他子目录层数。
- 图片、封面和其他非音频文件不会进入处理队列。
- DJ、DJ 版、伴奏、纯音乐和单乐器版本自动跳过，不进入成品曲库。
- DEMO、片段、试听版进入人工复核清单，不会漏掉，也不会自动发布。
- 同歌手同名歌曲只保留一个最佳源文件；歌手名末尾数字和 `(1)` 等序号会合并。
- 成品目录固定为 `G:\JuweierMusicProcessed\01_Ready\首字母\歌手\歌曲`，不创建专辑层。
- App 读取持久化数据库和已发布成品，不在每次打开时重新扫描磁盘。
- AI 工作目录放在服务器 E 盘，所有成品通过校验后才原子发布到 G 盘。
- 禁止使用频谱遮罩伪造电吉他；没有真实独立电吉他 UVR 模型时任务会明确停止，不发布不完整成品。

## 三首试跑

默认优先选择：

1. `D:\MP3\刘德华\练习.mp3`
2. `D:\MP3\张国荣\01 风继续吹.mp3`
3. `D:\MP3\郑秀文(1)` 下的 `02 值得.mp3`

所有扫描结果会同时写入 JSON 和 CSV 清单。待处理、跳过、重复以及人工复核歌曲均可追溯。

## 重要限制

`htdemucs_6s` 只能输出合并 Guitar，不能证明木吉他和电吉他已经独立。服务器必须安装并验证官方信息明确标注 `Electric Guitar` 或 `E.Guitar` 的 UVR 模型，才能开始正式发布。
