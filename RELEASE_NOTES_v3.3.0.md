# 橘味儿音乐 v3.3.0

本版把产品从“用户点歌后现场 AI 处理”调整为“服务器提前批量生产，三端直接使用成果”。

## 曲库与分类

- App 启动立即显示本地缓存歌曲名，不触发服务器硬盘扫描。
- 服务器独立维护目录索引，默认每 15 分钟后台增量检查；客户端只比较 `catalog_version` 并合并变化。
- 推荐、乐库、歌单、歌手、分类、AI 成果六个入口统一用于 Windows、Android 和 iOS。
- 支持语种、曲风、场景、榜单与成果状态多维分类。
- 歌手目录中的人工 A-Z 首字母会锁定并优先于自动拼音推断。
- 服务器断线时保留并显示上次完整索引，不清空曲库。

## 服务器批量生产

- Windows 管理端启动服务器队列后可以关闭客户端，服务器仍逐首处理。
- 支持安全暂停、失败重试和成功/失败记录清理。
- 完成分轨、BPM/调性、和弦、歌词时间轴、乐谱、编配后自动写入成果清单并更新曲库版本。
- 原始歌曲与 AI 成果目录可放在不同硬盘，数据库、模型和日志继续放在服务器 E 盘。

## 三项发布门槛

- 七个音频结果（含独立 `electric_guitar.wav`）必须是有效、时长对齐的 WAV。
- 电吉他六线谱必须存在。
- 逐字/逐词歌词时间轴必须映射到主旋律音符；播放器同时高亮当前歌词单元和当前音符。

未达到门槛的歌曲会标记为“成果待校验”或“歌词待校对”，不会伪装成完整 AI 成果。
